# Security Policy

Report a security problem through GitHub's private vulnerability reporting
feature when it is enabled. Do not place sensitive prefixes, router
configurations, credentials, or customer data in a public issue.

This application runs in the browser and queries the public RIPEstat Data API.
It does not request router credentials and does not store lookup history on a
J2SW server.
